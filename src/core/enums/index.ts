export enum ROUTES {
  HOME = '/',
  HEROES = '/heroes',
  LOCATIONS = '/locations',
  EPISODES = '/episodes',
  DYNAMIC_PATH = ':id',
  NOT_FOUND = '/404',
  ALL_PATH = '*',
}

export enum SORT_DIRECTION {
  ASC = 'ASC',
  DESC = 'DESC',
}
